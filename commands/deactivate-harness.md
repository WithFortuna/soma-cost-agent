Deactivate the Cost SOMA Policy Harness from this Codex project.

Use the `cost-soma-deactivate-project` skill.

Target selection:
- Use the current working directory as the target project by default.
- If the user supplied a path, use that path as the target.

Run the bundled deactivation script from the plugin root. Report the target path, hooks backup path if present, and confirm that unrelated hooks were left intact.
