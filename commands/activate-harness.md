Activate the Cost SOMA Policy Harness in this Codex project.

Use the `cost-soma-activate-project` skill.

Target selection:
- Use the current working directory as the target project by default.
- If the user supplied a path, use that path as the target.

Run the bundled activation script from the plugin root. Report the target path, state file path, hooks backup path if present, and smoke result.
